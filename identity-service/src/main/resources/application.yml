spring:

  application:
    name: identity-service

  datasource:
    url: jdbc:postgresql://localhost:5432/booksocial
    username: booksocial
    password: booksocial

  jpa:
    hibernate:
      ddl-auto: update

    open-in-view: false

server:
  port: 8081